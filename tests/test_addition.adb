with Ada.Text_IO; use Ada.Text_IO;
procedure Test_Addition is
-- VARIABLES
a, b, result : Integer;
begin
  a := 5;
  b := 3;
  result := a + b;  -- Ceci devrait utiliser ton nouveau code d'addition
end Test_Addition; eof
